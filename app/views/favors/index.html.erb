<%= link_to "Ask new favor", new_favor_path %>

<% unless @favors.empty? %>
  <p>You should
    <ul>
      <% @favors.each do |favor| %>
        <li><%= favor.contents %> to <%= favor.friend %></li>
      <% end  %>
    </ul>
  </p>
<% end %>
